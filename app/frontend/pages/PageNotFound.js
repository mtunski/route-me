import React from "react"

const PageNotFound = () => (
  <div>
    <p>
      Unfortunately, the page You were looking for does not exist.
      <br />
      You may have made a typo, or the page might have been moved.
    </p>
  </div>
)

export default PageNotFound
